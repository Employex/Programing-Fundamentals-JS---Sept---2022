function sumFirstAndLastArrayEllement(nums) {
  let firstElement = nums[0];
  let lastElement = nums[nums.length - 1];
  let result = firstElement + lastElement;

  console.log(result);
}
sumFirstAndLastArrayEllement([20, 30, 40]);
